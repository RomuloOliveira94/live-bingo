en:
  app:
    name: "Live Bingo!"
    tagline: "Live bingo caller"
    wordmark: "Live Bingo!"

  nav:
    cta:
      create: "Create"
      enter: "Join"

  layout:
    footer:
      credit_html: "Built with ❤️ by %{author_link}"
      author: "Rômulo Oliveira"

  pages:
    home:
      heading_line1: "Create a bingo"
      heading_line2: "room"
      subtitle: "Create a free game and get a code to share — or join an existing room."
      create: "Create bingo"
      enter: "Join with code"
    enter:
      title: "Join a bingo"
      heading_line1: "Join a"
      heading_line2: "bingo"
      subtitle: "Enter the code the organizer shared with you."
      code_label: "Bingo code"
      code_placeholder: "AB1-23C"
      code_invalid: "Invalid code — check with the organizer."
      submit: "Enter room"
      create_instead: "I want to create a bingo"
    join:
      not_found: "Invalid code — check with the organizer."

  games:
    show:
      title: "Bingo"
      finish_button: "Finish bingo"
      confirm_finish: "Are you sure you want to finish? This cannot be undone."
      confirm_restart: "Restart will clear all drawn numbers. Continue?"
      waiting:
        start_button: "Start drawing"
        eyebrow: "Room created"
        heading: "Call the crowd"
        guest_heading: "Waiting for the bingo to start"
        players_label:
          one: "player in the room"
          other: "players in the room"
        code_eyebrow: "Access code"
      active:
        status_line: "Room %{code} · live"
        draw_button: "Draw ball"
        drawing: "Drawing…"
        all_drawn: "All balls drawn"
        restart_button: "Restart game"
        recent_label: "Recent"
        board_label: "Full board"
        count_label: "%{drawn} / 75"
      finished:
        message: "This bingo has ended."
      share:
        copy_code: "Copy code"
        copy_code_done: "Code copied"
        share_invite_desktop: "Share invite"
        share_invite_mobile: "Share"
        payload_title: "Live Bingo!"
        payload_body: "Join my live bingo with code %{code}"
    errors:
      game_not_active: "Bingo must be active."

  draws:
    errors:
      game_not_active: "Bingo must be active to draw."
      no_numbers_remaining: "All 75 numbers have been drawn."

  helpers:
    clipboard:
      prompt: "Copy:"
    share:
      prompt: "Copy the link:"

  date:
    formats:
      default: "%d/%m/%Y"
  time:
    formats:
      default: "%d/%m/%Y at %H:%M"

  # English restatements of the pt-BR overrides in pt-BR.yml, kept here only
  # for parity-test coverage (see test/i18n_locale_parity_test.rb) — these
  # already match Rails' own bundled English validation defaults, so nothing
  # actually changes for the :en locale. activerecord.attributes.* is NOT
  # mirrored here; see EXCLUDED_NAMESPACES for why.
  activerecord:
    errors:
      messages:
        blank: "can't be blank"
        taken: "has already been taken"
        invalid: "is invalid"
        too_short: "is too short (minimum is %{count} characters)"
        too_long: "is too long (maximum is %{count} characters)"
        wrong_length: "is the wrong length (should be %{count} characters)"
        not_a_number: "is not a number"
        not_an_integer: "must be an integer"
        greater_than: "must be greater than %{count}"
        greater_than_or_equal_to: "must be greater than or equal to %{count}"
        less_than: "must be less than %{count}"
        less_than_or_equal_to: "must be less than or equal to %{count}"
        equal_to: "must be equal to %{count}"
        in: "must be in %{count}"
        between: "must be between %{count} and %{max}"
        present: "must be blank"
        accepted: "must be accepted"
        empty: "can't be empty"
        exclusion: "is reserved"
        inclusion: "is not included in the list"
        confirmation: "doesn't match %{attribute}"
        required: "must exist"
        record_invalid: "Validation failed: %{errors}"
        restrict_dependent_destroy:
          has_one: "Cannot delete record because a dependent %{record} exists"
          has_many: "Cannot delete record because dependent %{record} exist"
