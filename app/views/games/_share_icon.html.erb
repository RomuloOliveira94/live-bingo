<%# The share action's icon, from the design doc. Rendered from both the
  in-page share block (games/_share) and the navbar's compact game-code
  cluster (games/_nav_share) — kept in one place so the two never drift
  apart from each other.
%>
<svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
  <path d="M12 16V4"></path>
  <path d="M7 9l5-5 5 5"></path>
  <path d="M4 15v4a2 2 0 002 2h12a2 2 0 002-2v-4"></path>
</svg>
